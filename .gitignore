*.iml
.gradle
.idea/
# Track .claude/skills/ (agent capabilities ship with the template) but ignore
# local/machine state.
.claude/settings.local.json
.claude/settings.json
local.properties
.DS_Store

# Real device identifiers (serial / address / codename) stay LOCAL — never commit.
scripts/device-registry.local.sh
build/
.cxx/
captures/
__pycache__/

gradle/
gradlew
gradlew.bat

*.apk
*.aab
*.keystore
*.jks
keystore.properties

# Benchmark run outputs (device-harness.sh / run-bench.sh) — transient,
# device-specific. Keep them out of git; analysed + reported, not source.
results/

# Secrets
secrets.properties

# Local planning files
PLAN_*.md
